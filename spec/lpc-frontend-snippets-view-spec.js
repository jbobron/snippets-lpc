'use babel';

import LpcFrontendSnippetsView from '../lib/lpc-frontend-snippets-view';

describe('LpcFrontendSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
